import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'anslagstavla.hofors',
  appName: 'Hofors Anslagstavla',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
